import * as React from 'react';
import { Layout, Recommended } from '../../containers';

const RecommendedMovies: React.FC = () => (
  <Layout>
    <Recommended title="Recommended Movies" />
  </Layout>
);

export default RecommendedMovies;
