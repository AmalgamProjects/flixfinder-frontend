import * as React from 'react';
import { Layout } from '../../containers';

const ReccomendedTvShows: React.FC = () => (
  <Layout>
    ReccomendedTvShows
  </Layout>
);

export default ReccomendedTvShows;
