import * as React from 'react';
import { Layout } from '../../containers';

const ReccomendedMovies: React.FC = () => (
  <Layout>
    ReccomendedMovies
  </Layout>
);

export default ReccomendedMovies;
