import * as React from 'react';
import { Layout, Recommended } from '../../containers';

const RecommendedTvShows: React.FC = () => (
  <Layout>
    <Recommended title="Recommended TV Shows" />
  </Layout>
);

export default RecommendedTvShows;
